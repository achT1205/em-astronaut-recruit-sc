{
  "name": "hardhat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",  "scripts": {
    "deploy:dev": "npx hardhat run scripts/deploy.js --network localhost --show-stack-traces",
    "deploy:rinkeby": "npx hardhat run scripts/deploy.js --network rinkeby --show-stack-traces",
    "deploy:mumbai": "npx hardhat run scripts/deploy.js --network mumbai --show-stack-traces",
    "deploy:goerli": "npx hardhat run scripts/deploy.js --network goerli --show-stack-traces",
    "test": "npx hardhat test --show-stack-traces"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.1",
    "erc721a": "^4.2.0",
    "fs": "^0.0.1-security",
    "util": "^0.12.4"
  }
}
